# Config file for query generator
l1_billing_and_payments_rt_t_recharge_daily:
    where_clause: ""
    feature_list:
        number_of_top_ups: "count(*)"
        top_up_volume: "sum(face_value)"
    granularity: "date(recharge_date),access_method_num,date(register_date)"