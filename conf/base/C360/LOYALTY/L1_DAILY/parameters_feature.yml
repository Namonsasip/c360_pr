l1_loyalty_es_log_event_daily:
  feature_list:
    total_transaction: "count(es_event_id)"
  granularity: "es_event_id, partition_date"
  event_date_column: "to_date(cast(partition_date as string), 'yyyyMMdd')"
  join_column_with_cust_profile:
    access_method_num: es_event_id
    event_partition_date: event_partition_date
  output_catalog: "l1_loyalty_es_log_event_daily"
  partition_num_per_job: 5