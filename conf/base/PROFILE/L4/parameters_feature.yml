# Config file for query generator
l4_customer_profile_age_gender:
    where_clause: ""
    feature_list:
        access_method_num: "access_method_num"
        register_date: "register_date"
        age: "floor(months_between(to_date(cast(partition_month as STRING), 'yyyyMM'), to_date(birth_date))/12)"
        gender: "gender"
#        tenure_temp: "months_between(to_date(cast(partition_month as STRING), 'yyyyMM'), to_date(register_date))"
    granularity: ""

